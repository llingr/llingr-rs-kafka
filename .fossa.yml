version: 3
project:
  name: llingr-rs-kafka

# This crate is dual-licensed AGPL-3.0-only OR LicenseRef-Llingr-Commercial and
# statically links the Go engine, so FOSSA's Rust-crate scan sees only part of
# the picture. The Go dependencies embedded by static linking (franz-go and its
# submodules, klauspost/compress, pierrec/lz4, golang.org/x/crypto) are recorded
# in THIRD-PARTY-NOTICES, regenerated by gen-third-party-notices.sh on every
# engine bump. Every file is covered by an in-file SPDX header or the REUSE.toml
# bulk annotation.
